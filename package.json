{
  "name": "earnsignal-web-monorepo",
  "private": true,
  "workspaces": [
    "apps/app",
    "apps/site"
  ],
  "scripts": {
    "build:app": "npm --workspace @earnsigma/app run build",
    "build:site": "npm --workspace @earnsigma/site run build",
    "test:build-assertions": "node scripts/assert-builds.mjs"
  }
}
